#include <stdio.h>
#include <string.h>
/**
 * _strlen - give the length of a string
 * @s: pointer to string value
 *
 * Return: length
 **/
int _strlen(char *s)
{
	int len = 0;

	len = strlen(s);

	return (len);
}
