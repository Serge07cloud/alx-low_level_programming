#include <stdio.h>

/**
 * main - Entry point
 * Application: Numberz
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i = 48;

	while (i < 58)
	{
		putchar(i);
		i++;
	}

	/* New line */
	putchar(10)

	return (0);
}
